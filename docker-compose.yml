version: '3'
services:
  scraping-server:
    build:
      context: ./server
      dockerfile: Dockerfile
    image: scraping-server
    container_name: scraping-server

    volumes:
      - ./server/src:/src

    working_dir: /src

    tty: true